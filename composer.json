{
    "require": {
        "ext-calendar": "*"
    }
    
    "scripts": {
    "post-install-cmd": [
      "cd source/PHPMailer && composer.phar install",
      'cd source/GoogleAuth && composer require google/apiclient:"^2.0"'
    ]
  }
}
